{% extends "::empty.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block content %}

    <p>{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</p>
    <p><a href="{{ path('home') }}">{{ 'registration.back'|trans }}</a></p>
{% endblock content %}
