imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: services.yml }
    - { resource: "@UserBundle/Resources/config/services.yml" }
    - { resource: "@AppBundle/Resources/config/services.yml" }
    - { resource: "@ChatBundle/Resources/config/services.yml" }

# Put parameters here that don't need to change on each machine where the app is deployed
# http://symfony.com/doc/current/best_practices/configuration.html#application-related-configuration
parameters:
    locale: fr

framework:
    #esi:             ~
    translator:      ~
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    #serializer:      { enable_annotations: true }
    templating:
        engines: ['twig']
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # http://symfony.com/doc/current/reference/configuration/framework.html#handler-id
        handler_id:  session.handler.native_file
        save_path:   "%kernel.root_dir%/../var/sessions/%kernel.environment%"
    fragments:       ~
    http_method_override: true
    assets: ~
    php_errors:
        log: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"

# Doctrine Configuration
doctrine:
    dbal:
        driver:   pdo_mysql
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/../var/data/data.sqlite"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        naming_strategy: doctrine.orm.naming_strategy.underscore
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: "smtp"
    encryption: "ssl"
    port:      "465"
    host:      "smtp.gmail.com"
    username:  "esgigroupeone@gmail.com"
    password:  "pwdGROUPE1"
    spool:     { type: memory }

fos_user:
    db_driver: orm
    firewall_name: main
    user_class: UserBundle\Entity\User
    from_email:
        address: "esgigroupeone@gmail.com"
        sender_name: "esgigroupeone@gmail.com"
    registration:
        confirmation:
            enabled: true
            template: FOSUserBundle:Registration:email.txt.twig
            from_email:
                address: "esgigroupeone@gmail.com"
                sender_name: "Club des Critiques"
    resetting:
        email:
            template:   email/password_resetting.email.twig
            from_email:
                address: "esgigroupeone@gmail.com"
                sender_name: "Club des Critiques"
    service:
        mailer: fos_user.mailer.twig_swift
    profile:
        form:
          type: UserBundle\Form\UserEditType

# KNP MENU BUNDLE
knp_menu:
    # use "twig: false" to disable the Twig extension and the TwigRenderer
    twig:
        template: KnpMenuBundle::menu.html.twig
    #  if true, enables the helper for PHP templates
    templating: false
    # the renderer to use, list is also available by default
    default_renderer: twig


# VICH UPLOADER BUNDLE
vich_uploader:
    db_driver: orm
    mappings:
        cover_image:
            uri_prefix:         /upload/cover
            upload_destination: %kernel.root_dir%/../web/upload/cover
            namer:              app.cover.namer
            delete_on_update:   true
            delete_on_remove:   true
        avatar_image:
            uri_prefix:         /upload/avatar
            upload_destination: %kernel.root_dir%/../web/upload/avatar
            namer:              app.avatar.namer
            delete_on_update:   true
            delete_on_remove:   true


# GOS BUNDLE
#gos_web_socket:
#    server:
#        port: 8080        #The port the socket server will listen on
#        host: 127.0.0.1   #The host ip to bind to