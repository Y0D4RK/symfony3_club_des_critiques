<nav class="navbar navbar-inverse navbar-fixed-top menu">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand text-hide" href="{{ path('home') }}">Brand Text
            </a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            {{ knp_menu_render('AppBundle:Builder:mainMenu', {'currentClass': 'active'}) }}

            {% if knp_menu_render('AppBundle:Builder:categoryMenu', {'currentClass': 'active'}) == true %}
            <ul class="nav navbar-nav">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> Category <span class="caret"></span></a>
                    {{ knp_menu_render('AppBundle:Builder:categoryMenu', {'currentClass': 'active'}) }}
                </li>
            </ul>
            {% endif %}
            {{ knp_menu_render('AppBundle:Builder:logMenu', {'currentClass': 'active'}) }}
        <!--/.nav-collapse -->
        </div>
    <!--/.container-fluid -->
</nav>